<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns="http://www.w3.org/ns/widgets"
        xmlns:cdv="http://cordova.apache.org/ns/1.0"
        xmlns:android="http://schemas.android.com/apk/res/android"
        id="jp.ne.datt.jacichashsample"
        version="1.0.0">

    <name>JACIC Hash Sample</name>

    <description>
        Sample application for JACIC Hash Plugin - Embed and verify hash values in JPEG files
    </description>

    <author email="info@datt.ne.jp" href="https://www.datt.ne.jp">
        DATT JAPAN Inc.
    </author>

    <content src="index.html" />

    <access origin="*" />

    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="tel:*" />
    <allow-intent href="sms:*" />
    <allow-intent href="mailto:*" />
    <allow-intent href="geo:*" />

    <preference name="DisallowOverscroll" value="true" />
    <preference name="BackgroundColor" value="0xffffffff" />
    <preference name="Orientation" value="default" />

    <!-- iOS settings -->
    <platform name="ios">
        <allow-intent href="itms:*" />
        <allow-intent href="itms-apps:*" />

        <preference name="deployment-target" value="12.0" />

        <!-- iOS 11+ photo library usage description -->
        <edit-config target="NSPhotoLibraryUsageDescription" file="*-Info.plist" mode="merge">
            <string>JPEG画像にハッシュ値を埋め込むため、フォトライブラリへのアクセスが必要です。</string>
        </edit-config>
        <edit-config target="NSPhotoLibraryAddUsageDescription" file="*-Info.plist" mode="merge">
            <string>ハッシュ値が埋め込まれた画像を保存するため、フォトライブラリへのアクセスが必要です。</string>
        </edit-config>
    </platform>

    <!-- Android settings -->
    <platform name="android">
        <allow-intent href="market:*" />

        <preference name="android-minSdkVersion" value="21" />
        <preference name="android-targetSdkVersion" value="29" />
    </platform>

    <!-- Required Cordova Plugins -->
    <plugin name="cordova-plugin-whitelist" spec="^1.3.4" />
    <plugin name="cordova-plugin-file" spec="^6.0.2" />
    <plugin name="cordova-plugin-camera" spec="^6.0.0" />

    <!-- JACIC Hash Plugin -->
    <plugin name="cordova-plugin-jacic-hash" />

</widget>
